# Python
__pycache__/
*.py[cod]
*.pyc
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# Editors and OS
.idea/
.vscode/
.DS_Store

# Secrets: keep the template, never the real thing
.cagent.toml
# Any variant too: a ".bak" or ".local" copy of a config file holds the same
# key the original does, and is exactly what gets committed by accident.
.cagent.toml.*
!.cagent.example.toml

# Run artifacts
.cagent/
traces/
*.log
eval/runs/
